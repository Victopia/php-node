{
  "schedules": [
    // Sweeps expired data (e.g. emails, notifications ...) every 5 mins
    {
      "schedule": "*/5 * * * *",
      "command": "php .private/crons/GarbageCollector.php",
      "name": "gc"
    }
  ]
}
